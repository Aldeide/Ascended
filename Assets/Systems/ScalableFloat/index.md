# Scalable Float

This package defines the notion of a scalable float. A scalable float is defined by a base value and a curve that allows
the float to be evaluated as f = b + c(x) where x is between 0 and 1.